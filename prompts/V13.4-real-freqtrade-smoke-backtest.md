# AlphaPilot V13.4 - Real Freqtrade Smoke Backtest and Report Export

## 给 Codex 的执行说明

请严格按照本文档执行 V13.4。

本版本的目标是：**在真实 Docker / Freqtrade 环境中跑通第一轮冒烟回测，并把真实 Freqtrade 回测结果导出为 AlphaPilot 标准报告。**

本版本不是策略调参版本。  
本版本不是策略优化版本。  
本版本不是实盘版本。  
本版本不接真实 API Key。  
本版本不自动交易。  
本版本不创建真实订单。  
本版本只做真实数据下载、真实 Freqtrade backtest、报告转换、运行问题修复和文档记录。

完成后请输出完整自检报告。

---

# 1. Version Name

```text
AlphaPilot V13.4 - Real Freqtrade Smoke Backtest and Report Export
```

中文版本名：

```text
AlphaPilot V13.4 - 真实 Freqtrade 冒烟回测与报告导出版
```

---

# 2. Background

当前已完成：

```text
V13.1.3：AlphaPilot Mobile 真机问题修复，通过测试并打 tag
V13.2：AlphaPilot-Quant-Engine 后端骨架，已 push 并打 tag
V13.3：AlphaPilotVolumeReboundV01 策略实现，已 push 并打 tag
```

V13.3 已完成：

```text
AlphaPilotVolumeReboundV01.py 已从占位升级为可回测策略
已实现 EMA20 / EMA200 / RSI14 / MACD / volumeRatio / Bollinger Bands
已实现 BTC 3 根 15m 急跌过滤
已实现当前交易对 4h 趋势过滤
已实现 15m long-only entry
已实现 -3% stoploss
已实现 +3% take profit
已实现 MACD 两根走弱退出
已实现 12 根 15m 未盈利时间退出
已支持 Freqtrade JSON 转 AlphaPilot report
```

但 V13.3 环境没有 Docker，因此没有执行真实 Freqtrade 数据下载或真实 backtest。  
V13.3 只生成了 `isMock=true` 的 sample report，没有伪装成真实回测。

V13.4 要解决的核心问题：

```text
把 V13.3 的策略从“可回测代码”推进到“真实 Freqtrade 冒烟回测跑通”。
```

---

# 3. Core Goal

V13.4 的核心目标：

```text
1. 确认 Docker / Freqtrade 环境可用
2. 下载 BTC / ETH / SOL 的 OKX USDT 永续历史 K线
3. 先跑 BTC / ETH / SOL 的 smoke backtest
4. 生成真实 Freqtrade backtest result
5. 将真实结果转换为 AlphaPilot 标准报告
6. 报告必须标记 isMock=false
7. 如果 smoke 通过，再准备或执行 Top30 6个月回测
```

一句话目标：

```text
先跑通真实小范围回测流程，再考虑 Top30 大范围回测。
```

---

# 4. Required Mindset

请遵守以下原则：

```text
1. 先跑通，不调参
2. 先 smoke，不直接 Top30
3. 先真实报告，不伪造结果
4. 先确认流程，再评价策略
5. 不擅自修改已确认策略参数
6. 回测和实盘必须严格区分
7. 所有运行结果必须可追踪
8. 所有失败原因必须记录
```

本版本最重要的是确认：

```text
数据下载 → Freqtrade 回测 → JSON 结果 → AlphaPilot 报告导出
```

这条链路可以跑通。

---

# 5. Non Goals

V13.4 不做：

```text
不修改策略核心参数
不优化策略
不做参数搜索
不接真实 API Key
不接 Trade API
不接 Withdraw API
不读取真实账户
不读取真实持仓
不创建真实订单
不自动交易
不执行真实 dry-run
不接新闻系统
不接多模型
不接机器学习
不接多因子
不接资金费率模型
不接 OI / 持仓量
不接盘口深度
不做动态 Top30
不做全市场扫描
不做新币策略
不修改 AlphaPilot Mobile App
```

允许做：

```text
Docker / Freqtrade 环境检查
数据下载命令修复
backtest 命令修复
config 兼容修复
pair 格式修复
strategy runtime bug 修复
informative pair bug 修复
report export 修复
真实 backtest report 导出
文档更新
安全扫描
```

---

# 6. Hard Rules

必须遵守：

```text
1. 不保存真实 API Key
2. 不修改 config 为 live trade
3. 不打开真实交易
4. 不把 dry_run 改成 false
5. 不创建真实订单
6. 不接 Withdraw API
7. 不把 Freqtrade REST API 暴露到公网
8. 不提交 user_data/data 大量原始数据
9. 不提交大型 backtest 原始文件，除非文件很小并明确需要
10. 不把 mock report 伪装成真实报告
11. 不打 v13.4 tag，除非至少 smoke backtest 真实跑通并生成 isMock=false 报告
```

---

# 7. Repositories Scope

本版本主要修改：

```text
D:\Codex-Workspace\AlphaPilot-Quant-Engine
```

可修改 Docs 仓库，用于归档本提示词：

```text
D:\Codex-Workspace\AlphaPilot-Docs
```

不要修改 App 仓库主逻辑：

```text
D:\Codex-Workspace\trade-discipline-journal
```

除非只是文档说明，不要动 App 代码。

---

# 8. Preflight - Environment Check

进入 `AlphaPilot-Quant-Engine` 后，先执行：

```powershell
git status
docker --version
docker compose version
python --version
```

如果 Docker 不存在或 Docker Desktop 未启动：

```text
停止 V13.4 真实回测执行。
不要声称 V13.4 完成。
输出 blocked report。
说明需要安装 / 启动 Docker。
不要生成 isMock=false 报告。
不要打 v13.4 tag。
```

如果 Docker 存在，继续。

---

# 9. Preflight - Repository Check

检查当前仓库状态：

```powershell
cd D:\Codex-Workspace\AlphaPilot-Quant-Engine
git status
git branch
git log --oneline -5
```

要求：

```text
当前分支 main
工作区干净
远端 origin 正常
v13.3 tag 存在
```

如果工作区不干净，先列出差异并询问/说明，不要直接覆盖未知修改。

---

# 10. Preflight - Safety Check

先运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/check_safety.ps1
python -m alphapilot.scripts.validate_config
```

确认：

```text
没有真实 API Key
没有 live trade 配置
没有 dry_run=false
没有 Withdraw API
没有真实订单逻辑
```

如果 validate_config 失败，先修 config 安全问题，不进入回测。

---

# 11. Phase 1 - Smoke Data Download

## 11.1 Smoke Pairs

先只使用 3 个币：

```text
BTC/USDT:USDT
ETH/USDT:USDT
SOL/USDT:USDT
```

不要一上来 Top30。

## 11.2 Timeframes

下载：

```text
15m
1h
4h
```

说明：

```text
15m 用于主策略
4h 用于趋势过滤
BTC 15m 用于大盘急跌过滤
```

## 11.3 Timerange

建议先使用最近 30 到 90 天。

以当前日期 2026-07-04 为参考，可以用：

```text
20260401-
```

如果当前日期不同，可以选择最近 90 天的安全范围。

## 11.4 Execute Download

先打印命令：

```powershell
.\scripts\download_data.ps1 -Pairs "BTC/USDT:USDT,ETH/USDT:USDT,SOL/USDT:USDT" -Timeframes "15m,1h,4h" -Timerange "20260401-"
```

确认命令正确后再执行：

```powershell
.\scripts\download_data.ps1 -Pairs "BTC/USDT:USDT,ETH/USDT:USDT,SOL/USDT:USDT" -Timeframes "15m,1h,4h" -Timerange "20260401-" -Run
```

如果命令失败：

```text
1. 记录完整错误
2. 判断是 pair 格式、config、trading_mode、docker、OKX、Freqtrade 参数问题
3. 只修运行问题
4. 不改策略参数
```

---

# 12. Phase 2 - Smoke Backtest

## 12.1 Smoke Backtest Pairs

使用：

```text
BTC/USDT:USDT
ETH/USDT:USDT
SOL/USDT:USDT
```

## 12.2 Smoke Timerange

使用和下载相同或更短范围：

```text
20260401-
```

## 12.3 Run Command

先打印命令：

```powershell
.\scripts\run_backtest.ps1 -Timerange "20260401-" -Pairs "BTC/USDT:USDT,ETH/USDT:USDT,SOL/USDT:USDT"
```

确认无误后执行：

```powershell
.\scripts\run_backtest.ps1 -Timerange "20260401-" -Pairs "BTC/USDT:USDT,ETH/USDT:USDT,SOL/USDT:USDT" -Run
```

## 12.4 Runtime Bug Policy

如果 backtest 报错，可以修：

```text
策略 import 错误
Freqtrade API 兼容问题
informative pair merge 问题
pair 格式问题
config 字段问题
script 参数问题
report export 查找路径问题
```

不能修：

```text
不能因为结果不好修改止损
不能因为信号太少修改 RSI
不能因为信号太多修改 volumeRatio
不能因为亏损修改 take profit
不能调参
```

本版本只让回测跑起来。

---

# 13. Phase 3 - Export Smoke Report

如果真实 Freqtrade backtest 生成了结果 JSON，执行：

```powershell
python -m alphapilot.reports.export_backtest_report
```

导出的真实报告建议保存为：

```text
reports/smoke_backtest_report.json
reports/latest_backtest_report.json
```

要求：

```json
"isMock": false
```

必须包含：

```text
strategyId
strategyVersion
market
timeframe
universe
timerange
config
metrics
pairPerformance
monthlyPerformance
trades
skippedSignals
riskGateSummary
auditSummary
generatedAt
isMock
source
```

如果没有真实 Freqtrade 结果，只能生成：

```json
"isMock": true
```

并明确说明：

```text
没有真实 backtest result，报告为 mock。
```

绝不能把 mock report 当成真实报告。

---

# 14. Phase 4 - Top30 6-Month Prep / Optional Execution

只有 smoke backtest 跑通后，才进入 Top30。

## 14.1 Timerange

建议：

```text
20260101-
```

约等于最近 6 个月范围。

## 14.2 Download Top30 Data

先打印命令：

```powershell
.\scripts\download_data.ps1 -UseTop30 -Timeframes "15m,1h,4h" -Timerange "20260101-"
```

如脚本没有 `-UseTop30` 参数，需要在 V13.4 中补充。

确认后执行：

```powershell
.\scripts\download_data.ps1 -UseTop30 -Timeframes "15m,1h,4h" -Timerange "20260101-" -Run
```

## 14.3 Run Top30 Backtest

先打印命令：

```powershell
.\scripts\run_backtest.ps1 -UseTop30 -Timerange "20260101-"
```

确认后执行：

```powershell
.\scripts\run_backtest.ps1 -UseTop30 -Timerange "20260101-" -Run
```

## 14.4 Top30 Failure Policy

如果 Top30 因部分 pair 不支持失败：

```text
1. 记录失败 pair
2. 不直接删除全局 Top30
3. 为回测生成 temporary supported pairs list
4. 在报告中记录 excludedPairs
5. 不把删除 pair 静默当作策略优化
```

---

# 15. Report Export Improvements

如有必要，完善：

```text
alphapilot/reports/export_backtest_report.py
alphapilot/reports/backtest_report_schema.py
```

要求：

```text
1. 能找到最新 Freqtrade backtest JSON
2. 能识别 mock / real
3. 真实 report isMock=false
4. mock report isMock=true
5. 记录 source file
6. 记录 generatedAt
7. 记录 pairPerformance
8. 记录 tradeCount
9. 记录 maxDrawdownPct，如 Freqtrade 结果提供
10. 缺失字段使用 null，不伪造
```

如果某些指标 Freqtrade JSON 未提供：

```text
保留 null
在 reportWarnings 中说明
不要编造
```

---

# 16. Skipped Signal / Risk Gate Summary

V13.4 可以先做基础统计，不强求完整。

如果策略或 export 能提供跳过原因，记录：

```text
btc_crash_filter
weak_4h_trend
rsi_out_of_range
volume_ratio_too_low
macd_not_improving
price_too_extended
data_missing
```

如果当前无法提取，报告中写：

```json
"skippedSignals": [],
"riskGateSummary": {
  "available": false,
  "reason": "not implemented in V13.4"
}
```

不要伪造。

---

# 17. Docs To Add / Update

新增：

```text
docs/V13.4-real-freqtrade-smoke-backtest.md
docs/backtest-runtime-troubleshooting.md
docs/backtest-report-export.md
```

更新：

```text
README.md
docs/V13.3-volume-rebound-backtest.md
```

Docs 必须说明：

```text
V13.4 的目标是跑通真实 Freqtrade smoke backtest
V13.4 不是策略调参
V13.4 不实盘
V13.4 不接 API Key
V13.4 不自动交易
mock report 和 real report 的区别
```

---

# 18. Scripts To Update

可能需要更新：

```text
scripts/download_data.ps1
scripts/run_backtest.ps1
scripts/export_report.ps1
scripts/check_safety.ps1
```

要求：

```text
1. 支持 -UseTop30
2. 支持 -Pairs
3. 支持 -Timeframes
4. 支持 -Timerange
5. 默认只打印命令
6. 只有 -Run 才执行
7. 失败时 exit code 非 0
8. 输出清楚
```

---

# 19. Safety Requirements

必须保持：

```text
不接真实 Trade API
不接 Withdraw API
不保存真实 API Key
不读取真实账户
不读取真实持仓
不创建真实订单
不自动交易
不执行真实 dry-run
不暴露 REST API 到公网
不把 API 密码写死
```

允许：

```text
下载 public historical data
运行 Freqtrade backtesting
读取本地回测 JSON
导出 AlphaPilot report
修复 backtest runtime bugs
```

---

# 20. Testing Commands

必须运行：

```powershell
python -m compileall alphapilot
python -m alphapilot.scripts.print_project_status
python -m alphapilot.scripts.validate_config
python -m alphapilot.reports.export_backtest_report
powershell -ExecutionPolicy Bypass -File scripts/check_safety.ps1
git diff --check
```

如果 Docker 可用，运行：

```powershell
docker --version
docker compose version
.\scripts\download_data.ps1 -Pairs "BTC/USDT:USDT,ETH/USDT:USDT,SOL/USDT:USDT" -Timeframes "15m,1h,4h" -Timerange "20260401-" -Run
.\scripts\run_backtest.ps1 -Timerange "20260401-" -Pairs "BTC/USDT:USDT,ETH/USDT:USDT,SOL/USDT:USDT" -Run
python -m alphapilot.reports.export_backtest_report
```

如果 smoke 通过，再运行 Top30 可选流程。

---

# 21. Acceptance Criteria

V13.4 完成条件：

```text
1. Docker / Freqtrade 环境已检查
2. 如果 Docker 不可用，明确 blocked，未伪造完成
3. BTC / ETH / SOL smoke data download 成功，或失败原因明确
4. BTC / ETH / SOL smoke backtest 成功，或失败原因明确
5. 如果 backtest 成功，真实 Freqtrade result JSON 存在
6. 如果真实 result 存在，AlphaPilot report isMock=false
7. 如果没有真实 result，AlphaPilot report isMock=true 且明确说明
8. 不伪造真实回测指标
9. 不修改策略核心参数
10. 不接真实 API Key
11. 不接 Trade API
12. 不接 Withdraw API
13. 不读取真实账户
14. 不读取真实持仓
15. 不创建真实订单
16. 不自动交易
17. compileall 通过
18. validate_config 通过
19. safety scan 通过
20. git diff --check 通过
21. README / docs 更新
```

如果没有真实 smoke backtest 成功：

```text
不要打 v13.4 tag。
不要声称 V13.4 完成。
只提交 runtime fix / blocked docs 如必要。
```

如果真实 smoke backtest 成功：

```text
可以提交并打 v13.4 tag。
```

---

# 22. Commit And Tag Requirement

如果 smoke backtest 成功并生成真实 isMock=false report：

```bash
git add .
git commit -m "Run V13.4 real Freqtrade smoke backtest and export report"
git push
git tag v13.4
git push origin v13.4
```

如果只是修复脚本但没有跑通真实 backtest：

```bash
git add .
git commit -m "Prepare V13.4 Freqtrade smoke backtest runtime"
git push
```

但不要打 tag。

---

# 23. Self Review Output

完成后输出：

```text
1. 新增文件列表
2. 修改文件列表
3. 删除文件列表，如果没有删除写无
4. Docker 检查结果
5. Freqtrade 环境检查结果
6. Smoke data download 是否执行
7. Smoke data download 结果
8. Smoke backtest 是否执行
9. Smoke backtest 结果
10. 是否生成真实 Freqtrade result JSON
11. AlphaPilot report 路径
12. report isMock 值
13. Top30 data download 是否执行
14. Top30 backtest 是否执行
15. 失败 pair 列表，如果有
16. 是否修改策略参数
17. 是否修改策略 runtime bug
18. README 更新说明
19. docs 更新说明
20. 是否保存真实 API Key
21. 是否接 Trade API
22. 是否接 Withdraw API
23. 是否读取真实账户
24. 是否读取真实持仓
25. 是否创建真实订单
26. 是否自动交易
27. compileall 结果
28. validate_config 结果
29. print_project_status 结果
30. export_report 结果
31. safety scan 结果
32. git diff --check 结果
33. git status
34. commit hash
35. push 结果
36. tag 结果，如有
37. 已知问题
38. 下一步建议
```

---

# 24. Final Instruction

请严格执行 V13.4。

本版本核心是：

```text
真实 Freqtrade 冒烟回测
真实 backtest result
真实 AlphaPilot report export
```

不要调参。

不要实盘。

不要自动交易。

不要接真实 API Key。

不要伪造回测结果。

先 BTC / ETH / SOL smoke，再 Top30。

只有真实 smoke backtest 成功并导出 isMock=false 报告，才允许打 v13.4 tag。
