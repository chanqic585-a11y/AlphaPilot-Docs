# AlphaPilot V13.4.17 - Dynamic Regime Strategy Expanded Validation With Slippage and Liquidity Gate

## 给 Codex 的执行说明

请严格按照本文档执行 V13.4.17。

本版本目标：**在 V13.4.16 smoke 通过后，对 Dynamic Regime Strategy V0.1 做动态币种池扩大验证，并输出滑点调整、流动性闸门、概率评分、regime/module 分布报告。**

本版本不进入 Dry-run。  
本版本不实盘。  
本版本不接真实 API Key。  
本版本不自动交易。

---

# 1. Version Name

```text
AlphaPilot V13.4.17 - Dynamic Regime Expanded Validation
```

---

# 2. Precondition

必须确认：

```text
v13.4.16 tag 已存在
smoke report isMock=false
dynamic universe snapshots 存在
probability score table 存在
Docker / Freqtrade 可用
工作区干净
```

否则停止。

---

# 3. Scope

```text
Universe: historical dynamic universe snapshots
Timerange: 20260101-
Timeframe: 1h
Strategy: AlphaPilotDynamicRegimeV01
Slippage: 0.05%, 0.10%, 0.20%, 0.30% one-way stress
```

---

# 4. Required Work

```text
1. 使用动态 universe 回测，不用静态 Top30
2. 运行 expanded backtest
3. 生成 raw metrics
4. 生成 slippage-adjusted metrics
5. 生成 liquidity gate summary
6. 生成 regime/module breakdown
7. 生成 probability score bucket performance
8. 输出 dryRunApproved=false
```

---

# 5. Files To Add

```text
alphapilot/reports/generate_dynamic_regime_expanded_report.py
alphapilot/reports/dynamic_regime_expanded_schema.py
scripts/run_dynamic_regime_expanded_validation.ps1
reports/v13_4_17_dynamic_regime_expanded_report.json
reports/v13_4_17_dynamic_regime_expanded_summary.md
docs/V13.4.17-dynamic-regime-expanded-validation.md
```

---

# 6. Quality Gate

不批准 Dry-run，但报告判断：

```text
slippageAdjustedReturn > 0
slippageAdjustedPF > 1.15
maxDrawdown acceptable
maxConsecutiveLosses acceptable
no pair/month dominates
tradeCount meaningful
```

即使通过：

```text
dryRunApproved=false
nextStep = Shadow Trading readiness review
```

---

# 7. Commands

```powershell
cd D:\Codex-Workspace\AlphaPilot-Quant-Engine
git status
python -m compileall alphapilot
python -m py_compile user_data\strategies\AlphaPilotDynamicRegimeV01.py
.\scripts\run_dynamic_regime_expanded_validation.ps1 -Timerange "20260101-" -Run
python -m alphapilot.reports.generate_dynamic_regime_expanded_report
python -m alphapilot.scripts.validate_config
powershell -ExecutionPolicy Bypass -File scripts\check_safety.ps1
git diff --check
```

---

# 8. Acceptance Criteria

```text
1. expanded validation 成功或失败原因明确
2. 报告 isMock=false
3. raw metrics 存在
4. slippage stress metrics 存在
5. liquidity gate summary 存在
6. probability score summary 存在
7. regime/module breakdown 存在
8. dryRunApproved=false
9. 不接 API Key
10. 不自动交易
```

---

# 9. Commit

成功后：

```bash
git add .
git commit -m "Run V13.4.17 Dynamic Regime expanded validation"
git push
git tag v13.4.17
git push origin v13.4.17
```

未跑通不打 tag。

---

# Final Instruction

只做 expanded validation。

不要 Dry-run。

不要实盘。

不要调参。
