# AlphaPilot V13.4.18 - Dynamic Regime Signal Pipeline and Probability Gate Diagnosis

## 给 Codex 的执行说明

请严格按照本文档执行 V13.4.18。

本版本的目标是：**诊断 V13.4.17 Dynamic Regime expanded validation 中 tradeCount=0、probabilityScorePass=0、finalEntrySignals=0 的根因，确认问题到底来自动态币种池、Regime Router、模块候选信号、Probability Gate、Liquidity Gate、bucket key 匹配，还是实现层 bug。**

本版本不是调参版。  
本版本不是策略修改版。  
本版本不是回测优化版。  
本版本不是 Dry-run 版。  
本版本不是实盘版。  
本版本不接真实 API Key。  
本版本不自动交易。  
本版本不创建真实订单。  

完成后请输出完整自检报告。

---

# 1. Version Name

```text
AlphaPilot V13.4.18 - Dynamic Regime Signal Pipeline and Probability Gate Diagnosis
```

中文版本名：

```text
AlphaPilot V13.4.18 - 动态市场状态策略信号链路与概率闸门诊断版
```

---

# 2. Background

当前已经完成：

```text
V13.4.12：Dynamic Universe and Regime Strategy Specification
V13.4.13：Historical Dynamic Universe Builder
V13.4.14：Probability Score Dataset and Label Builder
V13.4.15：Dynamic Regime Strategy V0.1 Implementation
V13.4.16：Dynamic Regime Strategy Smoke Backtest
V13.4.17：Dynamic Regime Strategy Expanded Validation
```

V13.4.17 expanded validation 结果：

```text
isMock: false
pairCount: 27
tradeCount: 0
probabilityScorePass: 0
finalEntrySignals: 0
qualityGatePassed: false
dryRunApproved: false
liveTradingApproved: false
Backtest artifact: user_data\backtest_results\backtest-result-2026-07-04_21-28-28.zip
```

V13.4.17 结论：

```text
expanded runtime 跑通了；
dynamic universe 范围也跑通了；
但 probability gate 完全拦截；
没有任何 entry；
因此没有策略收益/亏损可评价；
不能进入 Dry-run。
```

现在必须先诊断：

```text
为什么没有任何 entry？
```

而不是直接放宽 probability gate 或修改策略。

---

# 3. Core Goal

V13.4.18 的核心目标：

```text
1. 读取 V13.4.17 expanded validation report
2. 读取 V13.4.14 probability score table
3. 读取 V13.4.13 dynamic universe snapshots/build report
4. 诊断整个信号漏斗：
   universe -> regime -> module candidate -> probability lookup -> probability pass -> liquidity gate -> final entry
5. 统计每一层通过/失败数量
6. 统计 probability bucket 覆盖率
7. 检查 probability score key 是否匹配
8. 检查是否存在实现层 bug
9. 输出诊断报告
10. 给出 V13.4.19 下一步建议
```

一句话目标：

```text
弄清楚是“策略太严格”，还是“概率表覆盖不足”，还是“字段匹配/实现 bug”，导致没有任何交易。
```

---

# 4. Required Mindset

请遵守以下原则：

```text
1. 先诊断，不放宽
2. 先定位漏斗断点，不直接改策略
3. 不因为 0 trade 就直接降低门槛
4. 不把无交易当作失败策略本身
5. 不伪造 signal count
6. 缺失数据必须写 unavailable
7. 所有结论必须来自 V13.4.13/V13.4.14/V13.4.17 报告或本地数据
8. 实盘与回测严格区分
```

---

# 5. Non Goals

V13.4.18 不做：

```text
不修改 AlphaPilotDynamicRegimeV01 策略逻辑
不修改 Probability Gate 阈值
不修改 probability score table
不修改 Dynamic Universe 规则
不修改 Regime Router 条件
不修改 Trend / Mean Reversion 模块条件
不运行新的参数搜索
不进入 Dry-run
不实盘
不接真实 API Key
不接 Trade API
不接 Withdraw API
不读取真实账户
不读取真实持仓
不创建真实订单
不自动交易
不修改 AlphaPilot Mobile App
```

允许做：

```text
新增诊断脚本
新增漏斗统计报告
新增 probability coverage report
新增 bucket key mismatch 检查
新增 docs
更新 README
如果发现纯实现 bug，可提出 hotfix 建议，但不要直接修改策略逻辑
```

---

# 6. Precondition

执行前必须确认：

```text
1. v13.4.17 tag 已存在
2. reports/v13_4_17_dynamic_regime_expanded_report.json 存在
3. reports/v13_4_17_dynamic_regime_expanded_summary.md 存在
4. reports/v13_4_14_probability_score_table.json 存在
5. reports/v13_4_13_dynamic_universe_build_report.json 存在
6. 当前工作区干净
```

如果不满足：

```text
停止 V13.4.18
提示先完成缺失版本
不要继续
```

---

# 7. Repository Scope

主要修改：

```text
D:\Codex-Workspace\AlphaPilot-Quant-Engine
```

可修改 Docs 仓库归档 prompt：

```text
D:\Codex-Workspace\AlphaPilot-Docs
```

不要修改 App 仓库主逻辑：

```text
D:\Codex-Workspace\trade-discipline-journal
```

---

# 8. Required Input Reports

必须读取：

```text
reports/v13_4_17_dynamic_regime_expanded_report.json
reports/v13_4_17_dynamic_regime_expanded_summary.md
reports/v13_4_14_probability_score_table.json
reports/v13_4_14_probability_dataset_report.json
reports/v13_4_13_dynamic_universe_build_report.json
reports/v13_4_13_dynamic_universe_summary.md
```

可读取：

```text
reports/v13_4_13_dynamic_universe_snapshots.json
reports/v13_4_13_dynamic_universe_sample_snapshots.json
user_data/backtest_results/backtest-result-2026-07-04_21-28-28.zip
```

如果文件缺失：

```text
缺失项写入 warnings。
不要伪造。
```

---

# 9. Signal Funnel Diagnosis

必须建立信号漏斗统计。

漏斗层级：

```text
1. universeSnapshots
2. selectedPairs
3. candlesEvaluated
4. dataReady
5. regimeClassified
6. trendRegimeCount
7. meanReversionRegimeCount
8. avoidRegimeCount
9. trendModuleCandidates
10. meanReversionModuleCandidates
11. probabilityLookupAttempts
12. probabilityLookupHits
13. probabilityLookupMisses
14. sufficientSampleBuckets
15. probabilityScorePass
16. liquidityGatePass
17. riskGatePass
18. finalEntrySignals
19. actualTrades
```

V13.4.17 已知：

```text
probabilityScorePass = 0
finalEntrySignals = 0
actualTrades = 0
```

V13.4.18 要定位：

```text
0 是从哪一层开始出现的？
```

---

# 10. Probability Gate Diagnosis

必须分析 probability gate。

## 10.1 Probability Table Coverage

统计：

```text
totalBuckets
bucketsWithSufficientSample
bucketsWithPositiveExpectancy
bucketsWithProfitFactorAbove1
bucketsWithProfitFactorAbove1_2
bucketsPassingCurrentGate
bucketSampleCountDistribution
```

## 10.2 Candidate Lookup Coverage

统计：

```text
probabilityLookupAttempts
lookupHits
lookupMisses
missRate
topMissingBucketKeys
```

## 10.3 Gate Failure Reasons

统计：

```text
insufficient_sample
profit_factor_too_low
expectancy_not_positive
hit_probability_too_low
bucket_not_found
bucket_key_mismatch
probability_table_unavailable
```

---

# 11. Bucket Key Consistency Check

必须检查 V13.4.15 策略生成的 bucket key 与 V13.4.14 probability score table 的 key 是否一致。

检查项：

```text
regime name 是否一致
liquidityBucket 是否一致
volatilityBucket 是否一致
rsiBucket 是否一致
emaDistanceBucket 是否一致
bbPositionBucket 是否一致
btcState 是否一致
大小写是否一致
分隔符是否一致
pair 格式是否一致
timeframe 是否一致
```

如果发现 mismatch：

```text
不要直接放宽 gate。
输出 keyMismatchReport。
建议 V13.4.19 做 bucket key normalization hotfix。
```

---

# 12. Regime Router Diagnosis

必须分析：

```text
trendCount
meanReversionCount
avoidCount
unknownCount
```

如果几乎全部是 avoid：

```text
可能 Regime Router 过严或数据缺失。
```

如果 trend/mean_reversion 有候选但 probability 全拦截：

```text
问题集中在 probability gate。
```

如果没有 trend/mean_reversion 候选：

```text
问题集中在 universe / regime / module rules。
```

---

# 13. Module Candidate Diagnosis

必须分析：

```text
trendModuleCandidateCount
meanReversionModuleCandidateCount
trendModulePassRate
meanReversionModulePassRate
moduleSkipReasons
```

如果 module candidates 为 0：

```text
Dynamic Regime Strategy 实际没有产生候选信号。
```

如果 module candidates > 0 但 probability pass = 0：

```text
Probability Gate 完全拦截。
```

---

# 14. Liquidity Gate Diagnosis

虽然 probabilityScorePass = 0，仍需检查 liquidity gate 是否参与拦截。

统计：

```text
liquidityGateAvailable
liquidityGateAttempts
liquidityGatePass
liquidityGateReject
liquidityDataUnavailableCount
```

如果 probability 已经全拦截，liquidity 可能没有机会执行。

要记录：

```text
liquidityGateNotReachedBecauseProbabilityFailed = true
```

---

# 15. Output Files

新增：

```text
alphapilot/reports/generate_dynamic_regime_signal_pipeline_diagnosis.py
alphapilot/reports/dynamic_regime_pipeline_diagnosis_schema.py
reports/v13_4_18_dynamic_regime_pipeline_diagnosis_report.json
reports/v13_4_18_dynamic_regime_pipeline_diagnosis_summary.md
docs/V13.4.18-dynamic-regime-pipeline-diagnosis.md
docs/probability-gate-coverage-diagnosis.md
docs/bucket-key-consistency-check.md
```

---

# 16. Report Schema

报告结构：

```json
{
  "reportId": "v13_4_18_dynamic_regime_pipeline_diagnosis",
  "sourceExpandedReport": "reports/v13_4_17_dynamic_regime_expanded_report.json",
  "sourceProbabilityTable": "reports/v13_4_14_probability_score_table.json",
  "currentStatus": "diagnosis_only",
  "dryRunApproved": false,
  "liveTradingApproved": false,
  "signalFunnel": {},
  "probabilityGateDiagnosis": {},
  "bucketKeyConsistency": {},
  "regimeRouterDiagnosis": {},
  "moduleCandidateDiagnosis": {},
  "liquidityGateDiagnosis": {},
  "rootCauseHypotheses": [],
  "recommendedNextStep": "",
  "warnings": [],
  "generatedAt": "UTC ISO"
}
```

---

# 17. Root Cause Categories

报告必须将根因归类为以下一种或多种：

```text
probability_gate_too_strict
probability_table_insufficient_coverage
bucket_key_mismatch
regime_router_too_strict
module_rules_too_strict
dynamic_universe_too_narrow
liquidity_gate_blocking
data_availability_issue
implementation_bug_suspected
unknown
```

---

# 18. Next Step Recommendation Logic

根据诊断输出下一步。

## Case A：bucket key mismatch

推荐：

```text
V13.4.19 - Probability Bucket Key Normalization Hotfix
```

## Case B：probability table insufficient coverage

推荐：

```text
V13.4.19 - Probability Bucket Coarsening and Sample Coverage Expansion
```

## Case C：probability gate too strict

推荐：

```text
V13.4.19 - Probability Gate Calibration Candidate Design
```

## Case D：module candidate count = 0

推荐：

```text
V13.4.19 - Regime Router / Module Candidate Logic Diagnosis
```

## Case E：dynamic universe has insufficient snapshots

推荐：

```text
V13.4.19 - Dynamic Universe Data Coverage Fix
```

## Case F：liquidity gate blocking

推荐：

```text
V13.4.19 - Liquidity Gate Calibration
```

---

# 19. Commands To Run

在 Quant 仓库执行：

```powershell
cd D:\Codex-Workspace\AlphaPilot-Quant-Engine
git status
python -m compileall alphapilot
python -m alphapilot.reports.generate_dynamic_regime_signal_pipeline_diagnosis
python -m alphapilot.scripts.validate_config
powershell -ExecutionPolicy Bypass -File scripts\check_safety.ps1
git diff --check
```

不运行 backtest。

---

# 20. README / Docs Update

README 增加：

```text
## V13.4.18 Dynamic Regime Pipeline Diagnosis
```

说明：

```text
V13.4.18 diagnoses why V13.4.17 produced zero final entries.
It does not change strategy logic.
It does not loosen the probability gate.
It does not run Dry-run.
```

中文说明：

```text
V13.4.18 用于诊断 V13.4.17 为什么没有产生任何 final entry。
本版本不修改策略逻辑，不放宽概率闸门，不进入 Dry-run。
```

---

# 21. Safety Requirements

必须保持：

```text
不接真实 Trade API
不接 Withdraw API
不保存真实 API Key
不读取真实账户
不读取真实持仓
不创建真实订单
不自动交易
不执行 Dry-run
不运行新 backtest
不修改 AlphaPilot Mobile App
```

允许：

```text
读取本地报告
读取本地 probability table
生成 pipeline diagnosis
更新 docs
```

---

# 22. Acceptance Criteria

完成后必须满足：

```text
1. 生成 pipeline diagnosis report
2. 生成 pipeline diagnosis summary
3. 报告包含 signalFunnel
4. 报告包含 probabilityGateDiagnosis
5. 报告包含 bucketKeyConsistency
6. 报告包含 regimeRouterDiagnosis
7. 报告包含 moduleCandidateDiagnosis
8. 报告包含 liquidityGateDiagnosis
9. 报告包含 rootCauseHypotheses
10. 报告给出 recommendedNextStep
11. dryRunApproved=false
12. liveTradingApproved=false
13. 不修改策略逻辑
14. 不放宽 probability gate
15. 不运行 backtest
16. 不进入 Dry-run
17. 不接真实 API Key
18. 不接 Trade API
19. 不接 Withdraw API
20. 不读取真实账户
21. 不读取真实持仓
22. 不创建真实订单
23. 不自动交易
24. compileall 通过
25. validate_config 通过
26. safety scan 通过
27. git diff --check 通过
```

---

# 23. Commit And Tag

如果检查通过：

```bash
git add .
git commit -m "Add V13.4.18 Dynamic Regime pipeline diagnosis"
git push
git tag v13.4.18
git push origin v13.4.18
```

---

# 24. Self Review Output

完成后输出：

```text
1. 新增文件列表
2. 修改文件列表
3. 删除文件列表，如果没有删除写无
4. 输入 expanded report 路径
5. 输入 probability table 路径
6. signal funnel 摘要
7. probability lookup attempts
8. probability lookup hits
9. probability lookup misses
10. probability score pass count
11. bucket key mismatch 结果
12. regime router 分布
13. module candidate count
14. liquidity gate 是否到达
15. root cause hypotheses
16. recommended next step
17. 是否修改策略逻辑
18. 是否放宽 probability gate
19. 是否运行 backtest
20. 是否进入 Dry-run
21. 是否保存真实 API Key
22. 是否接 Trade API
23. 是否接 Withdraw API
24. 是否读取真实账户
25. 是否读取真实持仓
26. 是否创建真实订单
27. 是否自动交易
28. compileall 结果
29. validate_config 结果
30. safety scan 结果
31. git diff --check 结果
32. commit hash
33. push 结果
34. tag 结果
35. 已知问题
36. 下一步建议
```

---

# 25. Final Instruction

请严格执行 V13.4.18。

本版本只诊断 V13.4.17 为什么没有产生任何交易。

不要修改策略。

不要放宽概率闸门。

不要运行回测。

不要进入 Dry-run。

不要实盘。

不要接 API Key。

不要自动交易。
