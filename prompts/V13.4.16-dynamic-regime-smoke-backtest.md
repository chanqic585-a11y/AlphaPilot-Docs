# AlphaPilot V13.4.16 - Dynamic Regime Strategy Smoke Backtest

## 给 Codex 的执行说明

请严格按照本文档执行 V13.4.16。

本版本目标：**对 V13.4.15 实现的 AlphaPilotDynamicRegimeV01 进行 BTC / ETH / SOL / 活跃 Top5 的真实 smoke backtest，验证动态币种池、regime router、trend/mean-reversion module、probability score、liquidity gate fallback 的运行链路。**

本版本不进入 Dry-run。  
本版本不实盘。  
本版本不接真实 API Key。  
本版本不自动交易。  

---

# 1. Version Name

```text
AlphaPilot V13.4.16 - Dynamic Regime Strategy Smoke Backtest
```

---

# 2. Precondition

必须确认：

```text
v13.4.15 tag 已存在
AlphaPilotDynamicRegimeV01.py 存在
reports/v13_4_13_dynamic_universe_* 存在
reports/v13_4_14_probability_* 存在
Docker / Freqtrade 可用
工作区干净
```

否则停止。

---

# 3. Scope

Smoke pairs：

```text
BTC/USDT:USDT
ETH/USDT:USDT
SOL/USDT:USDT
```

可选活跃 Top5 来自 V13.4.13 snapshots。

Timerange：

```text
20260401-
```

Timeframe：

```text
1h
```

---

# 4. Required Work

```text
1. 准备数据下载命令
2. 运行 smoke backtest
3. 生成真实 Freqtrade result
4. 导出 AlphaPilot smoke report
5. 报告 isMock=false
6. 报告 dryRunApproved=false
7. 统计 regime 分布
8. 统计 trend / mean_reversion entry 数量
9. 统计 probability score pass/fail
10. 统计 liquidity fallback 使用次数
```

---

# 5. Files To Add

```text
alphapilot/reports/generate_dynamic_regime_smoke_report.py
alphapilot/reports/dynamic_regime_smoke_schema.py
scripts/run_dynamic_regime_smoke_backtest.ps1
reports/v13_4_16_dynamic_regime_smoke_report.json
reports/v13_4_16_dynamic_regime_smoke_summary.md
docs/V13.4.16-dynamic-regime-smoke-backtest.md
```

---

# 6. Commands

```powershell
cd D:\Codex-Workspace\AlphaPilot-Quant-Engine
git status
python -m compileall alphapilot
python -m py_compile user_data\strategies\AlphaPilotDynamicRegimeV01.py
.\scripts\download_data.ps1 -Pairs "BTC/USDT:USDT,ETH/USDT:USDT,SOL/USDT:USDT" -Timeframes "1h,4h" -Timerange "20260401-" -Run
.\scripts\run_backtest.ps1 -Strategy "AlphaPilotDynamicRegimeV01" -Timeframe "1h" -Timerange "20260401-" -Pairs "BTC/USDT:USDT,ETH/USDT:USDT,SOL/USDT:USDT" -Run
python -m alphapilot.reports.generate_dynamic_regime_smoke_report
python -m alphapilot.scripts.validate_config
powershell -ExecutionPolicy Bypass -File scripts\check_safety.ps1
git diff --check
```

---

# 7. Acceptance Criteria

```text
1. smoke backtest 成功或失败原因明确
2. 如果成功，报告 isMock=false
3. dryRunApproved=false
4. 报告包含 tradeCount、return、DD、PF、winRate
5. 报告包含 regime breakdown
6. 报告包含 module breakdown
7. 报告包含 probability score summary
8. 报告包含 liquidity gate summary
9. 不接 API Key
10. 不自动交易
```

---

# 8. Commit

成功后：

```bash
git add .
git commit -m "Run V13.4.16 Dynamic Regime smoke backtest"
git push
git tag v13.4.16
git push origin v13.4.16
```

若未跑通，不打 tag。

---

# Final Instruction

只做 smoke backtest。

不要 Dry-run。

不要实盘。

不要调参。
